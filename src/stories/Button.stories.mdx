import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import { Button } from "../components/atoms/Button";

<Meta
  title="Components/Atoms/Button"
  component={Button}
  argTypes={{
    text: {
      control: {
        type: "text",
      },
    },
    disabled: {
      control: {
        type: "boolean",
      },
    },
    isPrimary: {
      control: {
        type: "boolean",
      },
    },
    isLoading: {
      control: {
        type: "boolean",
      },
    },
  }}
/>

# Button

export const Template = (args) => <Button {...args} />;

<Canvas>
  <Story name="Primary" args={{ text: "Primary", disabled: false, isPrimary: true, isLoading: false }}>
    {Template.bind({})}
  </Story>
  <Story name="Secondary" args={{ text: "Secondary", disabled: false, isPrimary: false, isLoading: false }}>
    {Template.bind({})}
  </Story>
    <Story name="Loading" args={{ text: "Loading", disabled: false, isPrimary: true, isLoading: true }}>
    {Template.bind({})}
  </Story>
    <Story name="Disabled" args={{ text: "Disabled", disabled: true, isPrimary: false, isLoading: false }}>
    {Template.bind({})}
  </Story>
</Canvas>