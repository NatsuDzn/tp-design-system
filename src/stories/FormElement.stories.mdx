import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import { FormElement } from "../components/molecules/FormElement";

<Meta
  title="Components/molecules/FormElement"
  component={FormElement}
  argTypes={{
    placeholder: {
      control: {
        type: "text",
      },
    },
    type: {
      control: {
        type: "radio",
        options: ["email", "password", "text", "tel"],
      },
    },
    label: {
      control: {
        type: "text",
      },
    },
    isValid: {
      control: {
        type: "boolean",
      },
    },
  }}
/>

# FormElement

export const Template = (args) => <FormElement {...args} />;

<Canvas>
  <Story
    name="Label input"
    args={{ type: "email", placeholder: "username@domain.com", label: "Email", isValid: true }}
  >
    {Template.bind({})}
  </Story>
</Canvas>