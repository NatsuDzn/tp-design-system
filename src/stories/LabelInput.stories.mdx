import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import { LabelInput } from "../components/molecules/LabelInput";

<Meta
  title="Components/Atoms/LabelInput"
  component={LabelInput}
  argTypes={{
    placeholder: {
      control: {
        type: "text",
      },
    },
    type: {
      control: {
        type: "radio",
        options: ["email", "password", "text", "tel"],
      },
    },
    label: {
      control: {
        type: "text",
      },
    },
  }}
/>

# LabelInput

export const Template = (args) => <LabelInput {...args} />;

<Canvas>
  <Story
    name="Label input"
    args={{ type: "email", placeholder: "username@domain.com", label: "Email" }}
  >
    {Template.bind({})}
  </Story>
</Canvas>